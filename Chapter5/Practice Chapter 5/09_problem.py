# can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Suraj", [1,2]}

"In Python, sets can only contain immutable (hashable) elements — that means once the element is added, it cannot be changed in place"
